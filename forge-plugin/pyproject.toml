[project]
name = "forge-doc-suggester-cgr"
version = "0.1.0"
description = "FORGE plugin: recommend Chainguard blogs and docs from SE prospect notes"
requires-python = ">=3.12"
dependencies = [
    "doc-suggester-cgr @ git+https://github.com/mbarretta/doc-suggester-cgr.git",
    "forge-core>=0.1.0",
]

[project.entry-points."forge.plugins"]
doc-suggester-cgr = "forge_doc_suggester_cgr.plugin:create_plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/forge_doc_suggester_cgr"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

# Local development overrides — these are only applied when working in the
# doc-suggester-cgr workspace directly (uv ignores them during remote git installs).
[tool.uv.sources]
doc-suggester-cgr = { path = ".." }
